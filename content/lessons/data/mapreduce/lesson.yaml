id: mapreduce
subject: data
difficulty: beginner
equation: T = \text{reduce}(\text{map}(\text{split}(D, k)))
concepts:
- MapReduce
- distributed computing
- split
- shuffle
- reduce
prerequisites:
- algorithms-and-complexity
related:
- parallel-computing
- column-storage-and-bigquery
visualization:
  type: map-reduce
  options: {}
primary: step
parameters:
  step:
    min: 0
    max: 3
    step: 1
    value: 0
variables:
- D
- k
- T
derivation:
- \text{split}(D, k) \to k \text{ independent chunks}
- '\text{map: each of the } k \text{ machines processes its own chunk}'
- '\text{reduce: combine the } k \text{ partial results into } T'
presets:
- step: 0
- step: 1
- step: 3
challenges:
- check: step
  target: 1
- check: step
  target: 3
- check: step >= 2
