id: image-filters
subject: ai
difficulty: beginner
equation: \text{out}(x,y) = \sum_{i=-1}^{1}\sum_{j=-1}^{1} w_{i,j}\cdot \text{in}(x+i, y+j)
concepts:
  - convolution
  - kernel
  - edge-detection
  - feature-map
prerequisites: []
related:
  - activation-functions
visualization:
  type: image-filter
  options: {}
parameters:
  kernel:
    min: 0
    max: 2
    step: 1
    value: 0
  pos:
    min: 0
    max: 483
    step: 1
    value: 0
variables:
  - which 3×3 pattern of weights slides across the picture
  - which pixel the window is centred on right now
derivation:
  - \text{a } 3\times3 \text{ window of weights } w_{i,j} \text{ sits over one pixel}
  - \text{out}(x,y) = \sum w_{i,j}\cdot \text{in}(x+i,y+j)
  - \text{slide the window one pixel over; repeat for every pixel}
charts: []
primary: kernel
presets:
  - kernel: 0
    pos: 483
  - kernel: 2
    pos: 483
  - kernel: 1
    pos: 200
challenges:
  - check: kernel == 0 and pos >= 480
  - check: kernel == 2 and pos >= 480
  - check: kernel == 1 and pos >= 90 and pos <= 400
